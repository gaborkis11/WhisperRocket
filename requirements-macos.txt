# WhisperRocket - macOS Dependencies
# Apple Silicon (M1/M2/M3) optimalizálva

# GUI
PySide6>=6.5.0

# Audio
sounddevice>=0.4.6
soundfile>=0.12.1

# Hotkey és Clipboard
pynput>=1.7.6
pyperclip>=1.8.2

# macOS Native integráció (opcionális, de ajánlott)
pyobjc-framework-Cocoa>=9.0
pyobjc-framework-ApplicationServices>=9.0
pyobjc-framework-AVFoundation>=9.0

# MLX Whisper (Apple Silicon GPU gyorsítás)
mlx>=0.5.0
mlx-whisper>=0.1.0

# Fallback: faster-whisper (CPU, ha MLX nem elérhető)
faster-whisper>=0.10.0

# HuggingFace (modell letöltés)
huggingface-hub>=0.20.0

# Egyéb
numpy>=1.24.0

# FONTOS: ffmpeg szükséges az audio feldolgozáshoz!
# Telepítés: brew install ffmpeg
